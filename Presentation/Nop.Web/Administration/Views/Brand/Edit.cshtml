@model BrandModel

@{
    //page title
    ViewBag.Title = T("Moveleiros.Admin.Catalog.Brands.EditBrandDetails").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("Brands");
}

@using (Html.BeginForm(null, null, FormMethod.Post, new { id = "brand-form" }))
{
    @Html.AntiForgeryToken()
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Moveleiros.Admin.Catalog.Brands.EditBrandDetails") - @Model.Name
            <small><i class="fa fa-arrow-circle-left"></i>@Html.ActionLink(T("Moveleiros.Admin.Catalog.Brands.BackToList").Text, "List")</small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.SaveContinue")
            </button>
            <span id="brand-delete" class="btn bg-red">
                <i class="fa fa-trash-o"></i>
                @T("Admin.Common.Delete")
            </span>
        </div>
    </div>
    @Html.Partial("_CreateOrUpdate", Model)
}
@Html.DeleteConfirmation("brand-delete")
